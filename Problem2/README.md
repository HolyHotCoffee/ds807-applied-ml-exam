# Problem 2

## Overview
Solution for DS807 exam — Problem 2.

## Files
- `main.py` — entrypoint
- `data_preprocessing.py` — preprocessing pipeline
- `modelClass.py` — model abstractions/classes
- `shallow_learning.py` — baseline/shallow models
- `visualization.py` — plots/figures

## Data
The original exam dataset is not included. Place the provided exam files in this folder (or adjust paths inside the code).

## How to run (optional)
Run `python3 main.py` from this folder after installing dependencies.
